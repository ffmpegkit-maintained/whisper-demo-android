# Android / Gradle build outputs
.gradle/
build/
app/build/
app/libs/
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Model file (too large, downloaded at runtime)
*.bin

# Built APK — bundles the licensed FFmpegKit native libraries, never commit it
# to this public repo (distribute it via Gumroad / a private channel instead).
*.apk

# Sample videos a developer drops in to test
samples/

# Test results / generated SRT files
whisper_test_results.txt
*.srt
